{
  "_comment": [
    "Registers a driver reads forever while still reporting telemetry.",
    "The host counts every failed modbus_read against the poll, so each one",
    "takes the driver offline on hardware that omits that register.",
    "",
    "This file recorded debt that already shipped: 497 addresses across 49 of",
    "the 50 Modbus drivers when it was first measured. It is now empty, so",
    "drivers/tests/test_absent_register_settles.py holds every driver to the",
    "rule with no exceptions -- a single entry appearing here again is a",
    "regression, not a budget.",
    "",
    "Check one driver with:",
    "  make absent-register-report ID=<id>"
  ],
  "drivers": {}
}
